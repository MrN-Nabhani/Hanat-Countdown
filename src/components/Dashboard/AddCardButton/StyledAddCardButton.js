import styled from "styled-components";
import Button from "../../common/Button/StyledButton";

const StyledAddCardButton = styled(Button)`
  position: fixed;
  right: 1rem;
  bottom: 1rem;
  padding: 1rem;
  box-shadow: 2px 2px 5px black;
`;

export default StyledAddCardButton;
