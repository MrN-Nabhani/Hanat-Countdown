import styled from "styled-components";

const Button = styled.button`
  border-radius: 6px;
  border: 1px solid;
  outline: none;
  &:hover {
    cursor: pointer;
    font-weight: bold;
  }
`;
export default Button;
